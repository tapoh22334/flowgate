# flowgate Environment Variables

# Required: Target repository to monitor (owner/repo format)
GITHUB_REPO=owner/repo

# Optional: Default execution mode (swarm or hive)
# Default: swarm
FLOWGATE_MODE=swarm

# Optional: Polling interval in seconds for issue watcher
# Default: 60
POLL_INTERVAL=60

# Optional: Number of parallel pueue tasks
# Default: 2
PUEUE_PARALLEL=2
